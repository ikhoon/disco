{
  "name": "disco",
  "version": "0.1.0",
  "description": "Discord activity CLI — read channels, threads, mentions, and search from the terminal",
  "type": "module",
  "bin": {
    "disco": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "bun x tsc --noEmit",
    "build": "bun build --compile --minify --outfile dist/disco src/index.ts",
    "install-local": "bun run build && mkdir -p \"$HOME/.local/bin\" && cp dist/disco \"$HOME/.local/bin/disco\""
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}
